$ Testing new features

likho("Hello World!");

dhoro res = 0;

$ loops
for i = 0 theke 5 tahole
    likho(2 ^ i);
    dhoro res = res + i;
thamo

likho(res);

for k = 0 theke 10 ekok 2 tahole
    jodi k == 4 tahole cholo;
    jodi k == 8 tahole berhou;
    likho(k);
thamo

$ go_dfz
$ if-else
jodi i == 4 tahole
    likho("IF er vitor");
nahoy
    likho("IF er baire");
thamo

$ object
dhoro obj = [1, "zara", "farhana", 1.20, "DJ"];
likho(obj);

$ take integer input
$ dhoro dj = INPUT_INT();
$ likho(dj + 4);

$ input string
$ dhoro str = INPUT();
$ likho(str * 4);


dhoro x = 4;
dhoro y = 14;
dhoro y = 2 * x - 20;

jodi x == 4 ebong y == 8 tahole
    likho("THIK ACHE");
nahoy
    likho("Somthing wrong hapening");
thamo

jodi not x != 4 tahole
    likho("PROJECT-150 was the best course of 1/2");
nahoy
    likho("There was some errors in your code");
thamo

$ functions
kaj add(x, y)
    pathao x + y;
thamo

dhoro a = 4;
dhoro b = 12;
dhoro c = add(a, b);
likho(c);

kaj square(n) 
    pathao n ^ 2;
thamo

likho("ENTER AN INTEGER");
dhoro x = INPUT_INT();
dhoro ans = square(x);
likho(ans);